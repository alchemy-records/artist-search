import React from 'react';
import PropTypes from 'prop-types';

function Release(props) {
    return (
        <div>
            
        </div>
    );
}

Release.propTypes = {

};

export default Release;

